# 活动记录 - 新手级注释增强

- 完成时间：2026-06-21 22:10:30 +08:00
- 目标：根据“纯新手看不懂专业名词和代码逻辑”的反馈，进一步加深项目中文注释和学习说明。
- 主要操作：
  - 加深 `src/intent_engine.py` 注释，解释意图、槽位、场景模式、环境语义、拒识、置信度和动作生成流程。
  - 加深 `src/data_builder.py`、`src/trainer.py`、`src/state_bridge.py` 注释，解释数据集、标签、TF-IDF、逻辑回归、Pipeline、模型保存和 JSON 状态文件。
  - 加深 `src/app.py`、`src/api_llm.py`、`src/voice_input.py`、`src/status_dashboard.py` 注释，解释 GUI 回调、线程、API、Prompt、JSON 校验、语音识别和 HTML 看板。
  - 加深 `linux_bigdata/` 侧车脚本注释，解释 JSONL、事件、批处理、流式窗口、Counter、offset 等概念。
  - 新增 `docs/新手读代码指南-名词解释与主流程.md`，提供阅读顺序、主流程、名词解释和答辩口径。
- 验证结果：
  - 已运行 `python -m compileall run_app.py run_console.py train_models.py src linux_bigdata`。
  - 所有 Python 文件均通过语法编译检查。
- 重要产物：
  - `docs/新手读代码指南-名词解释与主流程.md`
